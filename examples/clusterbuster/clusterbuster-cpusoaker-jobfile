# Run as
# clusterbuster -f clusterbuster-server-jobfile

pod-type=cpu
data-rate=-1
transfertime=10
report-format=raw
precleanup=1
no-report-object-creation
deps-per-namespace=2
processes=3
namespaces=4
