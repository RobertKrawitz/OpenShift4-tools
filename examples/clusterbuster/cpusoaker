# Run as
# clusterbuster -f clusterbuster-server-jobfile

cleanup
workload=cpu
workloadruntime=10
report-format=raw
precleanup=1
no-report-object-creation
deps-per-namespace=2
processes=3
namespaces=4
